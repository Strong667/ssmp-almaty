# GuestLayout - Документация

## Описание

`GuestLayout` - это кастомный layout для публичных веб-страниц, созданный на основе `AppLayout` из MoonShine. Он предназначен для отображения публичных страниц сайта с использованием компонентов MoonShine, но без элементов админ-панели.

## Расположение

```
app/MoonShine/Layouts/GuestLayout.php
```

## Основные особенности

### 1. Центрированный и упрощенный контент

Layout использует два важных атрибута для стилизации:

```php
protected bool $contentCentered = true;  // Центрирует контент на странице
protected bool $contentSimpled = true;  // Упрощенный стиль страницы
```

Эти атрибуты применяются в методе `build()`:
- `contentCentered` добавляет класс `layout-main-centered` к основному контейнеру
- `contentSimpled` добавляет класс `layout-page-simple` к фрагменту контента

### 2. Публичное меню навигации

В отличие от `MoonShineLayout`, который использует админское меню, `GuestLayout` переопределяет метод `getTopBarComponent()` для создания публичной навигации.

#### Структура TopBar:

```
┌─────────────────────────────────────────────────────────┐
│ [Логотип]  [Главная] [О нас]          [🌙 Переключатель темы] │
└─────────────────────────────────────────────────────────┘
```

#### Компоненты TopBar:

1. **Логотип** (`getLogoComponent()`) - отображается слева
2. **Публичное меню** - горизонтальное меню с ссылками:
   - "Главная" - ссылка на главную страницу (`route('home')`)
   - "О нас" - ссылка на раздел "О нас" (`route('about.*')`)
3. **Действия** - справа отображается переключатель темы

### 3. Отсутствие админского меню

Метод `menu()` возвращает пустой массив, так как для публичных страниц не требуется админское меню:

```php
protected function menu(): array
{
    // Для публичных страниц меню не нужно
    return [];
}
```

## Использование

### В Resources

Для использования `GuestLayout` в ресурсе, переопределите метод `getLayout()`:

```php
namespace App\MoonShine\Resources\Home;

use MoonShine\Laravel\Resources\ModelResource;

class HomeResource extends ModelResource
{
    protected function getLayout(): string
    {
        return \App\MoonShine\Layouts\GuestLayout::class;
    }
}
```

### В Pages

Для использования в отдельной странице, установите свойство `$layout`:

```php
namespace App\MoonShine\Resources\Home\Pages;

use App\MoonShine\Layouts\GuestLayout;
use MoonShine\Laravel\Pages\Page;

class HomePage extends Page
{
    protected ?string $layout = GuestLayout::class;
    
    // ...
}
```

## Структура компонентов

### Метод `build()`

Создает следующую структуру:

```
Layout
└── Html
    └── Body
        └── Wrapper
            ├── TopBar (публичное меню)
            └── Div (основной контейнер)
                └── Fragment (контент)
                    ├── Flash (уведомления)
                    ├── Content (основной контент страницы)
                    └── Footer (подвал)
```

### Метод `getTopBarComponent()`

Создает TopBar с тремя основными секциями:

1. **Логотип** - через `getLogoComponent()`
2. **Меню навигации** - публичные ссылки с активным состоянием
3. **Действия** - переключатель темы и другие элементы

#### Пример публичного меню:

```php
Fragment::make([
    Link::make(route('home'), 'Главная')
        ->icon('home')
        ->when(
            fn (): bool => Route::currentRouteName() === 'home',
            fn (Link $link): Link => $link->customAttributes(['class' => 'active'])
        ),
    Link::make('about', 'О нас')
        ->icon('information-circle')
        ->when(
            fn (): bool => str_starts_with(Route::currentRouteName() ?? '', 'about.'),
            fn (Link $link): Link => $link->customAttributes(['class' => 'active'])
        ),
])->class('menu menu--horizontal')->name('topbar-menu')
```

### Метод `topBarSlot()`

Позволяет добавлять дополнительные элементы в секцию действий TopBar:

```php
protected function topBarSlot(): array
{
    return [
        // Здесь можно добавить дополнительные элементы
        // Например: Locales::make(), Search::make() и т.д.
    ];
}
```

## Отличия от MoonShineLayout

| Характеристика | GuestLayout | MoonShineLayout |
|----------------|-------------|-----------------|
| Меню | Публичное навигационное меню | Админское меню с ресурсами |
| Контент | Центрированный и упрощенный | Стандартный layout админки |
| TopBar | Публичные ссылки | Админские элементы (Profile, Notifications) |
| Sidebar | Отсутствует | Присутствует с меню ресурсов |
| Guard | Использует `web` guard | Использует `moonshine` guard |

## Настройка

### Добавление новых пунктов меню

Чтобы добавить новые пункты в публичное меню, отредактируйте метод `getTopBarComponent()`:

```php
Fragment::make([
    Link::make(route('home'), 'Главная')->icon('home'),
    Link::make('about', 'О нас')->icon('information-circle'),
    Link::make(route('news'), 'Новости')->icon('newspaper'),
    Link::make(route('contact'), 'Контакты')->icon('phone'),
])->class('menu menu--horizontal')->name('topbar-menu')
```

### Изменение стилей

Для изменения цветовой схемы используйте метод `colors()`:

```php
protected function colors(ColorManagerContract $colorManager): void
{
    parent::colors($colorManager);
    
    $colorManager->primary('#1977cc'); // Цвет для публичных страниц
}
```

### Добавление дополнительных элементов в TopBar

Используйте метод `topBarSlot()` для добавления элементов в правую часть TopBar:

```php
protected function topBarSlot(): array
{
    return [
        Locales::make(), // Переключатель языков
        Search::make(),  // Поиск (если нужен)
    ];
}
```

## Примеры использования

### Пример 1: Главная страница

```php
namespace App\MoonShine\Resources\Home\Pages;

use App\MoonShine\Layouts\GuestLayout;
use MoonShine\Laravel\Pages\Page;
use MoonShine\MenuManager\Attributes\SkipMenu;

#[SkipMenu]
class HomePage extends Page
{
    protected ?string $layout = GuestLayout::class;
    
    public function getTitle(): string
    {
        return 'Главная';
    }
    
    protected function components(): iterable
    {
        return [
            // Компоненты страницы
        ];
    }
}
```

### Пример 2: Страница "О нас"

```php
namespace App\MoonShine\Resources\About\Pages;

use App\MoonShine\Layouts\GuestLayout;
use MoonShine\Laravel\Pages\Page;

class AboutPage extends Page
{
    protected ?string $layout = GuestLayout::class;
    
    public function getTitle(): string
    {
        return 'О нас';
    }
}
```

## Маршрутизация

Маршруты для публичных страниц должны использовать `web` guard через middleware:

```php
use App\Http\Middleware\UseWebGuard;
use App\MoonShine\Resources\Home\Pages\HomePage;

Route::middleware([UseWebGuard::class])->group(function () {
    Route::get('/', function () {
        return app(HomePage::class)->render();
    })->name('home');
});
```

## Зависимости

- `MoonShine\Laravel\Layouts\AppLayout` - базовый класс
- `MoonShine\UI\Components\Link` - для создания ссылок в меню
- `Illuminate\Support\Facades\Route` - для определения активного маршрута

## Примечания

1. **Guard**: Публичные страницы используют `web` guard вместо `moonshine` guard
2. **Меню**: Админское меню отключено, используется только публичная навигация
3. **Header**: В текущей версии Header компонент удален из build() для упрощения структуры
4. **Активное состояние**: Автоматически определяется по текущему маршруту

## См. также

- [MoonShineLayout](MoonShineLayout.md) - документация по админскому layout
- [Resources Documentation](../app/MoonShine/Resources/README.md) - документация по ресурсам
- [Pages Documentation](../app/MoonShine/Pages/README.md) - документация по страницам
